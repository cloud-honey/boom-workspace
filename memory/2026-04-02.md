# 2026-04-02 (목) — 일일 메모리

## 11:05 KST [sonnet] 모델 초기화 원인 분석 & MLX 인스턴스 정리

**마스터 문제:**
- 어제 밤에 모델을 변경했는데 아침에 보면 기본 모델(sonnet)로 초기화됨

**원인 파악:**
- 게이트웨이 로그 분석: `2026-04-01T22:35:12`에 SIGUSR1 신호로 강제 재시작 발생
- 재시작 직전 22:33:31에 설정 변경(config.patch) — 붐엘 MLX 하트비트 모델로 업데이트
- 게이트웨이 재시작 시 세션 메모리의 모델 오버라이드는 초기화됨

**정리 항목:**
1. ✅ Ollama 상태 확인: 모든 모델 언로드 상태
2. ✅ MLX 상태 확인: Qwen3-14B 정상 로드 (localhost:8000, 헬스 체크 OK)
3. ✅ MLX 인스턴스 충돌 해결:
   - 원인: 어제부터 실행 중인 orphaned 프로세스(PID 87011)와 PM2의 새 프로세스 간 포트 8000 충돌
   - 해결: PID 87011 강제 종료 + PM2 재시작 → 정상화
   - 현재: booml-mlx-server (PID 63112) 단일 인스턴스 운영 중

**예방책:**
- PM2로 관리되므로 향후 충돌 안 생김
- 선택사항: crontab에 월 1회 orphaned 프로세스 정리 추가 가능

**결론:**
게이트웨이의 자동 SIGUSR1 재시작이 설정 변경 시 발생하는 정상 동작입니다. 세션 모델 오버라이드는 재시작 후 초기화되므로, 영구 변경이 필요하면 openclaw.json의 primary 모델을 수정해야 합니다.
