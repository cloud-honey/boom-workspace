
## 08:32 KST cron 저장 경로 오류 수정
- 지시: 대시보드에 일일 리포트 업데이트 안 됨
- 원인: cron/jobs.json payload에 상대 경로로 설정되어 에이전트가 ~/workspace/openclaw-dashboard/reports/ 에 저장
- 대시보드는 ~/.openclaw/workspace/openclaw-dashboard/reports/ 를 읽어서 불일치 발생
- 조치 1: 오늘 리포트 파일 올바른 경로로 복사 (즉각 수정)
- 조치 2: ~/.openclaw/cron/jobs.json payload 경로를 절대 경로(/home/sykim/.openclaw/workspace/openclaw-dashboard/reports/)로 수정
- 백업: ~/.openclaw/cron/jobs.json.bak.20260309083200 (자동 생성)
- 결과: 내일부터 올바른 경로에 자동 저장됨

## 11:22 KST 대시보드 히스토리 필터 오류 수정
- 지시: 오늘 작업 항목이 히스토리에 안 보이는 문제 확인
- 원인: server/index.js 히스토리 필터가 `리포트|report|daily|일일|요약|summary` 단어 자체를 차단
  → "리포트 경로 수정" 같은 일반 작업 제목도 같이 차단됨
- 필터 존재 이유: 매일 07:50 일일 리포트 생성 항목이 히스토리에 반복 노출되는 걸 방지하기 위해 자체 추가된 것
- 수정: 필터를 `일일 리포트|daily report|데일리 리포트` 패턴으로 좁힘 (정확히 일치하는 경우만 제외)
- 파일: server/index.js 231번, 552번 줄
- 재발 방지: 히스토리에 안 보이는 항목 있으면 이 필터부터 확인할 것

## 11:10 KST — heartbeat 감시 설정 수정
- 지시: heartbeat agentId main→bamti 재변경, 붐 비활성 알림 조건 수정
- 작업 1: jobs.json (53329f9e) agentId main → bamti
- 작업 2: 붐 비활성 알림 조건 수정 — 진행 중 작업이 있는데도 2시간 비활성인 경우에만 알림 (작업 없으면 알림 안 함)
- 백업: ~/.openclaw/cron/jobs.json.bak.2026030911xxxx
- 결과: 다음 하트비트(30분 주기)부터 밤티가 감시 실행

## 11:40 KST 토큰 사용량 카드 개선
- 지시: 오늘/7일/전체 탭 선택, 상위 5개 기본 표시 + 더보기 버튼
- 작업:
  - server/index.js: /api/tokens?period=today|7d|all 엔드포인트 추가 (openclaw sessions --all-agents 기반)
  - OpenClawDashboard.jsx: 탭 UI, fetchTokens 함수, tokenShowAll 상태 추가
  - 토큰 카드 전면 재작성: 탭 버튼 + 더보기 버튼 + 세션 수 표시
- 결과: 오늘(18세션), 전체(46세션) 기간별 조회 정상 확인
  - 오늘: claude 463,593 / deepseek 177,642
  - 전체: claude 1,025,369 / deepseek 177,642 / qwen 12,479

## 11:44 KST 세션 정리 + 중복 방지 수정
- 지시: 중복 세션 정리, heartbeat 재검토, 자동 정리 확인
- 문제 발견:
  1. heartbeat sessionTarget: isolated → 30분마다 :run:xxx 세션 신규 생성, 34개 누적
  2. cleanup.sh에 --enforce 없어 warn 모드만 작동 → 실제 삭제 안 됨
- 조치:
  1. sessions.json 수동 정리: 46 → 14개 (cron:run 32개 삭제, 최신 2개만 유지)
     - 백업: agents/main/sessions/sessions.json.bak.*, agents/bamti/...
  2. jobs.json heartbeat (53329f9e) sessionTarget: isolated → main (재사용, 세션 누적 방지)
  3. cleanup.sh: `openclaw sessions cleanup --all-agents` → `--enforce` 추가
- 결과: 다음 heartbeat부터 새 세션 생성 안 됨, 매시간 cleanup이 실제 삭제 실행

## 12:03 KST 토큰 절약 설정 개선
- 지시: 토큰 절약 설정 확인 및 개선
- 현황 확인: compaction:safeguard, memoryFlush:on, tools.profile:messaging, subagents.maxConcurrent:2
- 개선 1: heartbeat 프롬프트 1,289자→549자 (57% 감소) + thinking:off 추가
- 개선 2 (실패): compaction mode: safeguard→auto 시도 → auto는 허용값 아님 (default|safeguard만 허용)
  → 게이트웨이 설정 오류로 다운 → 마스터가 직접 default로 수정 후 복구
  → 교훈: openclaw.json 수정 전 반드시 허용값 CLI로 확인
- 현재 compaction: default (safeguard보다 더 자주 압축, 더 유리)

## 12:29 KST 모델 상태 카드 개선
- 지시: 항상 "정상"으로만 표시되는 모델 상태 카드를 실용적으로 개선
- 작업:
  - server/index.js: /api/models-status에 세션 기반 실시간 데이터 추가
    - openclaw sessions --all-agents --json → 모델별 오늘 토큰/마지막 활동/에이전트 자동 감지
    - deepseek 잔액 경고 임계값: $1 이하 warn, $0.5 이하 error
    - fmtLastActive() 헬퍼 추가 (방금 전/N분 전/N시간 전)
    - sessions 구조 수정: d.sessions.recent → Object.values(d.sessions) (--all-agents 응답 구조 상이)
  - OpenClawDashboard.jsx: 모델 카드 UI 전면 개선
    - 3행 구조: 모델명+상태 / 상세정보+마지막 활동 / 오늘 토큰+쿨다운
    - modelStatusStyle에 icon 필드 추가
    - usedBy 뱃지 스타일 추가
- 결과: claude 190,886토큰/3분 전, deepseek 86,389토큰/$3.23/20분 전 정확히 표시

## 12:39 KST allowed 모델 목록 중복 삭제
- 지시: 중복 모델(gemini-flash-latest) 삭제
- 작업: openclaw.json agents.defaults.models에서 google/gemini-flash-latest 삭제
- 결과: 10개 → 9개, 토큰 절약 효과는 미미 (나머지 6개는 비상용으로 유지)

## 12:03 KST heartbeat lightContext + daily-reporter thinking 개선
- 작업:
  - heartbeat cron: lightContext: true 추가 (부트스트랩 컨텍스트 최소화)
  - daily-reporter cron: thinking: low → off (gpt-5-mini는 thinking 무의미)

## 14:29 KST 미래교역 홈페이지 제안서 제작
- 지시: 시안A v2 기반 인포그래픽 스타일 제안서 제작
- 작업: proposal.html 생성 (pj.abamti.com/proposal.html)
  - 프로젝트 개요 (숫자 카드), 페이지 구성, 제품 카테고리, 디자인 시스템, 주요 특징, 작업 현황 타임라인
- 이후 수정:
  - 타임라인 순서 재정렬 (시안A v2 완료 → 시안 확정 → 작업 예정 3개 → 대기중 3개 → 관리툴)
  - 완료 항목 → 작업 예정, 대기중 뱃지 스타일 통일 (회색 동그라미 + 파란 뱃지)
  - 최하단 시안 보러가기 버튼 추가 (https://pj.abamti.com/sinan-a/)
  - 관리툴 카드 + 기존 제품 정보 전체 이전 안내 추가
  - 각 항목 상태값 수정 (2번 검토중, 6·7·8번 대기중)
- 결과: 완료

## 15:xx KST 시안A v2 Hero 배경 이미지 적용
- 지시: Hero 섹션에 실제 이미지 적용
- 작업:
  - 집진기 설비 이미지(image_bcbcdf39) 좌우 반전 후 hero-bg.jpg 저장
  - CSS 그라디언트 오버레이 조정 (좌 0.88 → 중 0.78 → 우 0.20)
  - 모바일: background-position: right center 고정
- 결과: sinan-a-v2/index.html Hero 배경 이미지 적용 완료

## 15:xx KST 시안A v2 쇼핑몰 영역 수정
- 지시: 쇼핑몰 버튼/텍스트 안 보이는 문제 수정
- 원인: .shop-link CSS가 어두운 배경(푸터)용 흰색 계열 색상 사용
        → 밝은 배경의 쇼핑몰 섹션에서 텍스트/테두리 안 보임
- 수정:
  - shop-config.js script 태그 누락 추가
  - .shop-link: 흰 카드 + 네이비 텍스트 (#1B3A6B) + 하늘색 테두리 (#d0d8e8)
  - 푸터 내 shop-link는 `footer .shop-link` 선택자로 기존 스타일 유지
- 결과: 쇼핑몰 버튼 정상 표시 확인

## 15:xx KST 창립연도 수정
- 지시: 창립연도 1999 → 2002 수정 (mrtrading.co.kr 연혁 기준 2002년 8월)
- 수정: badge + stat-num, about.html 연혁 기준
- 결과: sinan-a-v2 전반 2002년으로 통일

## 17:29 KST Claude Rate Limit 발생 → 자동 폴백
- 원인: anthropic:manual에 rate_limit 2회 발생 → 5분 쿨다운 (17:38~17:43 KST)
- 폴백 모델: google/gemini-2.5-pro (자동 전환)
- 쿨다운 종료: 17:43 KST (정상 복귀)
- 특이사항: 세션 model 필드가 폴백 모델로 덮어써져 세션 단위 claude/gemini 사용량 분리 불가
- 참고: Claude Max 요금제도 분당/시간당 속도 제한 있음, API 콘솔에서 사용량 0으로 표시 (세션 토큰 방식이라 API 콘솔에 집계 안 됨)

## 21:36 KST 대시보드 에이전트 카드 — 붐4 미표시 수정
- 지시: 밤티 서브에이전트 작업 상태 확인 + 붐4 표시 안 되는 문제 수정
- 확인: agent:bamti:subagent:4973e21e 세션 활성 (홈페이지 수집 작업 중)
- 원인: 프론트엔드가 /api/status의 agentsData를 사용 중인데, 해당 객체에 붐4 없음
- 수정:
  - OpenClawDashboard.jsx: load() 함수에 /api/agents 별도 호출 추가
  - agentsList state 추가, agentsList 우선 사용 → /api/status 폴백
- 결과: 붐4 포함 5개 에이전트 모두 대시보드에 정상 표시
- 빌드/재시작: npm run build + pm2 restart boom-dashboard 완료
