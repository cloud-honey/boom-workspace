# 2026-04-04 일일 메모리

## 인계 상황 (2026-04-03 밤 → 04-04 아침)

### 붐엘 자막 추출 작업 진행 중
- **배경**: 04-03 22:40 붐엘에 mlx-whisper 자막 추출 기능 추가
- **서버**: PM2 `booml-mlx-server` → `server_v3_postgres_router.py` (포트 8000)
- **봇**: PM2 `booml-telegram-bot` → `/Users/sykim/.openclaw/workspace/booml-telegram-bot/booml-bot-v2-enhanced.py`
- **whisper bin**: `booml-mlx/venv/bin/mlx_whisper`

### 자막 추출 완료 (04-04 07:09 확인)
- ssis-313-C.srt (8,940줄 원본) → ssis-313-C_KO.srt (9,319줄 한국어 번역) ✅
- base 모델 재추출 → 번역 완료

---

## 07:07 KST [sonnet] 히스토리 업데이트
- 지시: 마스터 "히스토리 업데이트" 요청
- 작업: 2026-04-04.md 생성, 04-03 인계 내용 정리
- 결과: 완료

## 07:28 KST [sonnet] 붐엘 TurboQuant 활성화
- 지시: 붐엘 TurboQuant 활성화
- 원인: 기존 코드는 kv_bits=4만 넘기고 kv_quant_scheme 미지정 → uniform quantization으로 처리됨
- 핵심 발견: mlx_vlm에 TurboQuant 내장됨 (turboquant.py), turboquant_enabled()는 scheme="turboquant"일 때만 True
- 작업: model_router.py 수정
  - 이전: kv_bits=4 (try/except로 묻혀있었음, uniform scheme)
  - 이후: kv_bits=3.5, kv_quant_scheme="turboquant"
  - 3.5비트: 메모리 4.6x 압축 + 품질 균형
- PM2 재시작: booml-mlx-server ✅
- 결과: ✅ TurboQuant 3.5bit KV 캐시 압축 활성화 완료

## 08:11 KST [sonnet] 붐엘 토큰 통계 수집 수정
- 지시: "붐엘 통계가 안쌓여"
- 원인: MLXAdapter.generate()가 문자열만 반환 → GenerationResult의 토큰 통계 버려짐
- 작업: 체인 전체 수정 (MLXAdapter → generate_with_strategy → booml_core → server)
  - MLXAdapter.generate(): (text, token_stats) 튜플 반환으로 변경
  - generate_with_strategy(): Tuple[str, str, Dict] 반환
  - booml_core._generate_response(): token_stats 수집 및 metadata에 포함
  - server_v3: usage 필드에 실제 토큰 수 채움
- 수정 파일: model_router.py, booml_core.py, server_v3_postgres_router.py
- 결과: ✅ usage.prompt_tokens=894, completion_tokens=9, generation_tps=72.3 t/s

## 08:19 KST [sonnet] 붐엘 /status TurboQuant 표시 수정
- 지시: "붐엘에서 /status 쳤을때 TurboQuant 비활성화 표시"
- 원인: 봇이 /health 응답의 `turboquant` 키를 읽는데 서버 HealthResponse에 해당 필드 없음
- 작업: server_v3_postgres_router.py HealthResponse 수정
  - turboquant: bool = True 추가
  - model_loaded: bool 추가 (어댑터 is_loaded 반영)
  - performance: Dict 추가 (model_id, load_time, kv_cache_bits: 3.5, kv_quant_scheme: turboquant)
- 결과: ✅ /status → TurboQuant: ✅ 활성 (4.6x 메모리 절약) 정상 표시

---

## 붐엘 프로젝트 현재 상태 (2026-04-04 08:23 기준)

### 서버 구성
| 항목 | 값 |
|------|---|
| PM2 프로세스 | booml-mlx-server (id:17), booml-telegram-bot (id:18) |
| 서버 스크립트 | booml-mlx/server_v3_postgres_router.py |
| 봇 스크립트 | booml-telegram-bot/booml-bot-v2-enhanced.py |
| 서버 포트 | 8000 |
| 텔레그램 봇 | @boomllm_bot |
| interpreter (봇) | venv/bin/python |
| interpreter (서버) | python3 (시스템) |

### 모델 & 성능
| 항목 | 값 |
|------|---|
| 모델 | mlx-community/gemma-4-26b-a4b-it-4bit |
| 아키텍처 | 26B MoE (8전문가, 2활성) |
| 생성 속도 | ~69-72 t/s |
| 프롬프트 처리 | ~500 t/s |
| 피크 메모리 | ~16.5-16.8 GB |
| KV 캐시 | TurboQuant 3.5bit (4.6x 압축) |

### 기능 현황
| 기능 | 상태 |
|------|------|
| TurboQuant KV 캐시 | ✅ 3.5bit, kv_quant_scheme="turboquant" |
| 토큰 통계 수집 | ✅ prompt/completion/total_tokens + tps |
| mlx-whisper 자막 추출 | ✅ /transcribe 폴더, 자막추출 파일 |
| 실시간 웹 검색 | ✅ DuckDuckGo |
| 피드백 시스템 | ✅ 버튼 + 텍스트 |
| 텔레그램 통합 | ✅ @boomllm_bot |
| /status 정상 표시 | ✅ TurboQuant/model_loaded/performance |

### 주요 파일 경로
```
/Users/sykim/.openclaw/workspace/booml-mlx/
  server_v3_postgres_router.py   # 메인 서버
  model_router.py                # MLX 어댑터 + TurboQuant
  booml_core.py                  # 코어 로직 (토큰 통계 포함)
  booml-config.json              # 설정
  venv/                          # Python 가상환경

/Users/sykim/.openclaw/workspace/booml-telegram-bot/
  booml-bot-v2-enhanced.py       # 텔레그램 봇 메인
```

### 알려진 이슈 / TODO
- [ ] 422 Unprocessable Content 에러 반복 (하트비트 등 잘못된 형식 요청)
- [ ] model_loaded: False → 첫 메시지 후 True (지연 로드 구조)

## 09:06 KST [sonnet] 미사용 도구 비활성화
- 지시: 토큰 절약 위해 미사용 미디어 도구 제거
- 작업: openclaw.json main 에이전트에 deny 추가
  - deny: image_generate, tts, canvas, pdf
  - 유지: image (이미지 분석), 나머지 전부
- 결과: ✅ 게이트웨이 재시작 완료

## 09:18 KST [sonnet] 토큰 절약 설정
- 지시: 토큰 절약 셋팅
- 작업:
  1. 백업/로그 파일 archives 이동 (~55KB 제거)
     - MEMORY-backup-original.md, MEMORY-backup-20260401.md
     - TOOLS-backup x2, SYNC_LOG.md
  2. AGENTS.md 슬림화: 10KB → 2KB
  3. openclaw.json 설정 변경:
     - bootstrapTotalMaxChars: 150k → 50k
     - bootstrapMaxChars: 기본값 → 15k
- 결과: ✅ 다음 세션부터 bootstrap 로드량 대폭 감소

## 09:21 KST [sonnet] 붐엘 번역 웹검색 오라우팅 버그 수정
- 지시: 번역 작업 응답 없음 → 중지 후 버그 수정
- 원인: translate_srt가 /v1/chat/completions로 번역 프롬프트 전송 시
  get_realtime_data가 "?" 감지 → DuckDuckGo 웹 검색 실행
- 수정: server_v3_postgres_router.py
  - "You are", "Translate" 로 시작하거나 500자 초과 메시지는 검색 제외
- 결과: ✅ 번역 요청 시 불필요한 웹 검색 없음

## 09:23 KST [sonnet] 워크스페이스 찌꺼기 파일 정리
- 지시: 찌꺼기 파일 정리
- 작업: memory/archives/old-scripts/ 로 이동
  - booml-bot.py, booml-bot-simple.py (구버전 봇)
  - cleanup_boom2_sessions_v2/v3.py (일회성 스크립트)
  - mlx-benchmark.py, benchmark_result.json (벤치마크)
  - Modelfile x6 (LLM/ 폴더로 이미 관리됨)
  - booml-archive-20260402/ → memory/archives/
- 결과: ✅ 워크스페이스 루트 정리 완료

## 09:40 KST [sonnet] 붐엘 자막 기본 모델 변경 + 환각 방지 옵션
- 지시: 기본값 base로 변경 + 어제 VAD 기능 적용
- 확인: VAD는 mlx_whisper CLI 미지원 → hallucination_silence_threshold로 대체
- 수정: server_v3_postgres_router.py
  - 기본 모델: tiny → base
  - `--word-timestamps True` + `--hallucination-silence-threshold 2` 추가
- 수정: booml-bot-v2-enhanced.py 기본 모델 표시 tiny→base
- 결과: ✅

## 09:50 KST [sonnet] 번역 키 불일치 버그 수정 확인
- 확인: 추출→번역 파이프라인 전체 연결 확인
- 발견: 단일 파일 `/transcribe` 시 번역 결과 키 불일치
  - 봇: `tr.get("ko_srt_path")` → 서버: `"output"` 키로 반환
- 수정: `tr.get("output", tr.get("ko_srt_path", ""))` 으로 폴백 처리
- 결과: ✅

## 09:52 KST [sonnet] /cancel 명령 추가
- 지시: 자막 작업 중간 취소 기능
- 구현: 전역 취소 플래그 (_cancel_requested) + /cancel 커맨드
  - 현재 파일 완료 후 다음 파일부터 중단 (SRT 깨짐 방지)
  - BotCommand 목록에 추가
- 결과: ✅ /cancel 로 언제든 중단 가능

## 09:56 KST [sonnet] 자막 기본 모델 base + 환각 방지 옵션 적용 (재정리)
- 어제 커밋 없이 진행된 VAD 옵션이 사라진 것 확인
- mlx_whisper CLI VAD 미지원 확인 → hallucination_silence_threshold로 대체
- 적용: --word-timestamps True + --hallucination-silence-threshold 2
- 기본 모델: tiny → base (봇/서버 양쪽)

## 10:06 KST [sonnet] 붐엘 자막 작업 일지 기능 추가
- 기능: 자막+번역 완료 시 subtitle-work-log.md 자동 기록
- 저장 위치: /Volumes/seot401/subtitle-work-log.md
- 기록 내용: 파일명, 등록일, 작업시작, 모델, 파일크기, 블록수, 글자수, 소요시간, SRT경로
- 적용 범위: /transcribe 폴더, /transcribe 파일, 자막추출 텍스트 명령 3곳 모두
- 파일명 한글→영어 변경 (인코딩 오류 방지)

## 10:12 KST [sonnet] /cancel 명령 + 파이프라인 안전 중단
- 기능: 진행 중 자막 작업 취소
- 방식: 전역 플래그 → 현재 파일 완료 후 다음 파일 시작 전 중단
- BotCommand 목록 등록

---

## 10:45 KST [sonnet] 붐2 세션 정리
- 서브에이전트 10개 삭제 (3/30~4/3 완료 작업)
- 백업 파일 4개 + .deleted 마커 4개 삭제
- 남은 세션: 7개 (크론 4개 + 다이렉트 1개 + 이전 실행 2개)

## 10:50 KST [sonnet] 하트비트 모델 확인
- 현재 설정: `heartbeat.model = "booml-mlx/booml-mlx"` (30분마다)
- 붐엘 서버 꺼지면 heartbeat failed 상태 됨

## 11:17 KST [sonnet] 붐엘 봇 버그 수정
- 문제: 자막 작업 중 봇이 28번 재시작 → 완료 알림 못 받음
- 원인: 409 Conflict (PM2 재시작 시 이전 인스턴스 미종료)
- 수정 내용:
  1. `transcribe_file()` / `translate_srt_file()` 최대 3회 재시도 추가
  2. 3회 실패 시 마스터 텔레그램 알림
  3. 파이프라인 오류 발생 시 대기(중단)
  4. `/translate [srt경로]` 명령어 추가 (SRT 한국어 번역)
  5. handle_message에서 `/` 슬래시 명령 필터 추가
  6. ffprobe 영상 길이 기반 추출 예상 시간 표시

## 11:44 KST [sonnet] /translate 명령 파싱 버그 수정
- 문제: 한글 파일명 포함 경로에서 CommandHandler 대신 handle_message가 처리
- 수정: raw 텍스트에서 직접 파싱 + handle_message 슬래시 명령 early return

## 11:44 KST [sonnet] IPX-219 번역 직접 요청
- curl로 서버에 직접 `/v1/translate_srt` 요청
- 3559 블록 → 355회 배치 × ~4초 = 약 45분 소요
- 12:29 KST 완료: `IPX-219_KO.srt` (507KB) ✅

## 13:22 KST [sonnet] TO-DO
- 붐엘 자막/번역 완료 시 마스터 알림 기능 추가 예정 (현재 작업 완료 후)

---

## 16:47 KST [sonnet] 붐엘 자막 작업 현황 확인
- 작업 일지 확인: STARS-120, PPPD-481, ssni-432, SOD Stars-094 완료
- ssni-432: 블록 0개 (번역 미완료) — srt 파일 위치 오류
- 실패 패턴: Whisper subprocess 오류, 빈 srt, 409 Conflict 봇 재시작

## 17:17 KST [sonnet] SRT 환각 제거 + 검증 기능 추가
- 문제: 자막 반복 환각 → 플레이어 오류 발생
- 수정 내용:
  1. `clean_srt()` 함수 추가 (server_v3_postgres_router.py)
     - 연속 동일 블록 최대 2회 제한
     - 블록 내 반복 문장 정리
     - 블록 0개/10개 미만 경고
  2. transcribe 완료 후 자동 클리닝 + 검증 실행
  3. `/v1/clean_srt` 엔드포인트 추가
  4. 봇 `/clean [srt경로]` 명령 추가
- ⚠️ 실수: 작업 중인 whisper 프로세스 확인 안 하고 서버 재시작 → 마지막 작업 중단
- 교훈: 서버/봇 재시작 전 반드시 `ps aux | grep mlx_whisper` 확인

## 17:22 KST 현재 진행 상황
- 파이프라인 완료: FC2-2386297, STARS-120, PPPD-481, ssni-432, SOD Stars-094 (5개)
- 마지막 파일 작업 중단됨 → /transcribe 재실행 필요
- TO-DO: 번역/자막 완료 알림 기능 추가 (대기 중)
