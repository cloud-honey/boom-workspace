# 2026-03-18 작업 기록

## 17:03 KST EV Highway v0.2.0 구현 시작

- 지시: 구현 지시서(EV_Highway_v0_2_0_Implementation_Prompt.md), 아키텍처 설계서(Charger_Realtime_Status_Architecture_v1_0.md), test-charger-stat.js 3개 파일 기반 v0.2.0 구현
- 배경: 환경공단 API에서 SK일렉링크(busiId=ST) 전부 stat=9(상태미확인) → SK일렉링크 자체 웹 API로 교체 필요

---

## 17:16 KST Step 1~3 완료

### shared/types.ts 타입 추가
- `ChargerStatus`, `NormalizedCharger`, `RouteChargerRestArea`, `RouteChargersResponse` 추가
- 기존 `VehicleData`, `TokenPair`, `ActiveRoute` 등 기존 타입 전부 유지

### SK일렉링크 KEY 매핑 (실조회 완료)
- `/map/marker` API로 5개 휴게소 KEY 확정:
  - 기흥(부산) → `ST41460034`
  - 칠곡(서울) → `ST47850007`
  - 여주(강릉) → `ST41670012`
  - 문막(인천) → `ST42130017`
  - 횡성(인천) → `ST42730003`

### charger-cache.ts 구현
- KECO 60초, SK일렉링크 30초, 매핑 24시간 차등 TTL

---

## 17:21 KST Step 4~7 완료 (서버 빌드 성공)

### 신규 파일 생성
- `server/src/services/sk-electlink-adapter.ts` — SK일렉링크 API 어댑터 (CHARGING_UNIT 폴백 포함)
- `server/src/services/keco-adapter.ts` — 환경공단 어댑터 (NormalizedCharger 정규화)
- `server/src/services/route-charger-service.ts` — 경로 기반 선별 호출 (앞쪽 N개)
- `server/src/data/sk-electlink-stations.ts` — SK일렉링크 KEY 매핑

### route-chargers API 추가
- `GET /api/highway/route-chargers?routeId=&lat=&lon=&count=` 엔드포인트 신설

### 클라이언트 수정
- DemoPage: 30초 간격으로 `/api/highway/route-chargers` 호출
- SimulationOverlay: "충전기 상태: 실시간 (환경공단 + SK일렉링크)" 라벨 추가

### 초기 검증 결과
- 기흥휴게소: KECO 2기 + SK일렉링크 8기 = 총 10기, UNKNOWN 0기 ✅

---

## 17:25 KST 버전 표기 수정
- TeslaConnectPage.tsx `v0.1.10` → `v0.2.0`
- 빌드/배포 완료

---

## 17:30 KST [밤티] 전국 SK일렉링크 매핑 완성 (붐3)

- 지시: /map/marker API 실조회 데이터 60개 → 노선별/방향별 분류
- 작업: `server/src/data/sk-electlink-stations.ts` 전체 재작성
- 결과:

| 노선 | 충전소 수 |
|------|----------|
| 경부선 (gyeongbu) | 6개 |
| 영동선 (yeongdong) | 9개 |
| 남해선 (namhae) | 7개 |
| 서해안선 (seohaeang) | 6개 |
| 중부선 (jungbu) | 4개 |
| 중부내륙선 (jungbunaelyuk) | 5개 |
| 새만금포항선 (saemangeum) | 5개 |
| 통영대전선 (tongdae) | 3개 |
| 상주영천선 (sangju_yeongcheon) | 3개 |
| 울산선 (ulsan) | 3개 |
| 동해선 (donghae) | 3개 |
| 청송영덕선 (cheongsong) | 2개 |
| **전체** | **60개** |

- 문제: restAreaId 체계 불일치 (`gyeongbu_01` vs `ra01`)
- 해결: `route-charger-service.ts` RouteRestArea에 `skStationKey` 직접 추가
- 빌드 성공 ✅

---

## 17:35~17:55 KST SK일렉링크 모달 버그 수정

### 문제 1: 모달 SK일렉링크 미표시
- 원인: `charger-detail.ts`가 `restAreaId='ra01'`로 매핑 조회 → 새 파일 ID `gyeongbu_01`과 불일치
- 해결: statId에 `ST*` 있으면 `findSKMappingByKecoStatId`로 직접 조회
- 강릉대관령: `restAreaId='yd07'` → SK_STATION_MAP에 alias 추가 + kecoStatId 설정

### 문제 2: 모달 터치 스크롤 불가
- 오버레이에 `touchAction: 'none'` → 내부 스크롤까지 차단
- bottom sheet 방식 폐기 → **전체화면 레이어 팝업**으로 전환
- `position: fixed + overflowY: auto` — 브라우저 기본 스크롤 사용

### 문제 3: 목록 짤림
- 그룹 컨테이너 `overflow: 'hidden'` 제거
- 사업자명 색상 테이블에 `기후에너지환경부`, `브라이트에너지파트너스`, `이지차저` 추가

### 전체 휴게소 검증 결과 (6개 SK 연동 휴게소)
| 휴게소 | SK일렉링크 | 전체 | 상태 |
|--------|-----------|------|------|
| 기흥 (ra01) | 8기 350kW | 10기 | ✅ |
| 칠곡 (ra07) | 5기 200kW | 9기 | ✅ |
| 여주맛있는 (yd03) | 6기 350kW | 18기 | ✅ |
| 문막 (yd04) | 2기 350kW | 3기 | ✅ |
| 횡성 (yd05) | 4기 350kW | 5기 | ✅ |
| 강릉대관령 (yd07) | 2기 350kW | 16기 | ✅ |

---

## 19:32 KST 환경공단 API 429 (일일 한도 초과)

- 원인: 개발 중 테스트 과정에서 하루 1,000건 한도 소진
- 기존 동작: 429 → Mock 데이터 표시 (문제)
- 해결:
  1. **KECO 캐시 TTL 1시간** (개발 중, 프로덕션에서는 60초로 자동 전환)
  2. **429 감지 시 isMock 대신 SK일렉링크만 실데이터 표시**
  3. **팝업에 `⚠️ 환경공단 API 일일 한도 초과` 얼럿 표시**
  4. **dataSources, cacheInfo, 마지막 갱신 시각** 팝업 하단 표시
  5. PM2 ev-highway-api: `bash -c npm run dev` → `node dist/server/src/index.js` 직접 실행으로 교체

### 최종 확인
- `kecoLimited: True` 시: isMock=False, dataSources=['SK_ELECTLINK'], SK 실데이터 정상 ✅
- `⚠️ 환경공단 API 일일 한도 초과 · SK일렉링크는 실시간 · 나머지 자정 이후 갱신` 얼럿 정상 표시

---

## 최종 상태
- 배포: https://evn.abamti.com (v0.2.0)
- PM2: ev-highway (id:10), ev-highway-api (id:12)
- git: branch main, 최종 커밋 `1bc3dee`
- 환경공단 API: 자정 리셋 후 정상 (내일 00:00 KST)

## 내일 할 일
- [ ] 자정 후 환경공단 API 정상 동작 확인
- [ ] 전체 휴게소 실데이터 검증 (KECO + SK 합산)
- [ ] 환경공단 API 운영 계정 할당량 증가 신청 검토 (data.go.kr 활용사례 등록)
