# 2026-04-01 작업 기록

## 09:45 KST EV Highway 토큰 만료 버그 수정
- 지시: 마스터가 차량 인증 후 정보가 안 뜬다고 보고
- 원인: DB 토큰 8개 전부 `REFRESH_FAILED` (3월 19일 이후 전부 만료)
  - 자동 갱신 3회 실패 후 영구 포기 → 복구 로직 없음
  - 프론트가 401 응답을 무시 → 사용자에게 재인증 안내 안 됨
- 작업:
  1. `useVehiclePolling.ts` — 401 감지 → tokenState 반환
  2. `DashboardPage.tsx` — 재인증 필요 시 빨간 배너 + "Tesla 다시 연동하기" 버튼
  3. `token-refresh.ts` — 갱신 시작 5분→15분 전, 실패 허용 3→5회, 최종 실패 시 🚨 로그
- 결과: 빌드 성공, PM2 재시작 완료, 커밋 c888f44
- 마스터 방침: 일시적 재시도 로직 안 넣음 (애초에 안 생기게 + 실패 시 알림)

## 10:00 KST 데일리 리포트 크론 프롬프트 수정
- 지시: 리포트 내용 부실, 어제와 중복 많음, 지수 데이터가 어제 것
- 원인 분석:
  1. Yahoo Finance `range=5d` → `chartPreviousClose`가 5일 전 값 → 변동률 왜곡 (코스피 -9.03%는 가짜)
  2. web_search에 날짜 제한 없음 → 오래된 뉴스 반복 (OpenAI 상장, TSMC 완판 등)
  3. 07:30 코스피는 장 시작 전인데 그냥 표시
- 수정 내용:
  1. `range=5d` → `range=2d`로 변경 → 정확한 전일 대비 변동률
  2. web_search에 `freshness="day"` 파라미터 필수 적용
  3. 검색 쿼리에 오늘 날짜 포함 + 1주일 이상 된 뉴스 절대 금지 규칙
  4. 07:30 크론: 코스피 "전일 종가 (장 시작 전)" 명시
  5. 18:00 크론: 코스피 "당일 종가" 명시
  6. 오전/오후 간 동일 내용 반복 금지 규칙 추가
- 결과: 07:30 크론(d98aa0fc), 18:00 크론(82a58f09) 둘 다 업데이트 완료

## 10:30 KST [opus] 데일리 리포트 시장 데이터 소스 교체
- 지시: 코스피 전일 종가 부정확 → 소스 교체 + 금/비트코인 추가
- 작업:
  - 코스피: Yahoo Finance → 네이버 금융 API (m.stock.naver.com) 교체
  - 미국지수/WTI/금: Yahoo Finance chartPreviousClose → close 배열 기반 전일 종가 계산
  - 비트코인: CoinGecko API 신규 추가
  - 시장 항목: 5개 → 7개 (코스피, S&P, 나스닥, 다우, WTI, 금, 비트코인)
  - 아침(07:30) + 오후(18:00) 크론 모두 업데이트
- 결과: ✅ 완료, 테스트 실행 정상

## 11:00 KST [opus] 대시보드 히스토리 표시 수정
- 지시: 대시보드 히스토리 "내용 없음" 표시 문제 수정
- 원인:
  - server/index.js __INITIAL_STATE__ 스크립트 HTML 미주입 (변수만 생성하고 삽입 안 됨)
  - real_openclaw_proxy.js XPS 경로 잔재 (/home/sykim → /Users/sykim)
- 작업:
  - server/index.js: __INITIAL_STATE__ </head> 앞 주입 코드 추가
  - real_openclaw_proxy.js: 맥미니 경로 + loadHistory() 추가
  - vite.config.js: /api 프록시 설정 추가 (개발 환경용)
  - dist/assets 이전 빌드 잔재 js 7개 삭제
  - vite build 재실행
- 결과: ✅ API 26일, __INITIAL_STATE__ 25일 히스토리 정상 반환
- git push: a3b3d7ef

## 13:57 KST [opus] 맥미니 DarkWake 잠자기 문제 진단 및 해결
- 지시: 2시간 넘게 모든 서버 다운, SSH 접속 불가 상태 보고
- 원인: macOS DarkWake — displaysleep 후 시스템이 잠자기 상태 진입
  - caffeinate 실행 중이었지만 DarkWake 방지 불가
  - pmset 로그에서 Sleep/Wake 패턴 반복 확인
- 작업: `sudo pmset -a displaysleep 10 sleep 0 disksleep 0 powernap 0 womp 1 ttyskeepawake 1 tcpkeepalive 1`
  - ttyskeepawake 1: 터미널 세션 있으면 시스템 잠들지 않음
  - tcpkeepalive 1: 네트워크 연결 유지
  - displaysleep 10: 모니터만 10분 후 꺼짐
- 결과: ✅ 마스터가 직접 실행 완료, 설정 적용 확인

## 14:00 KST [opus] 대시보드 히스토리 content 파싱 버그 수정
- 지시: 어제/그저께 히스토리 상세 내용 안 나오는 문제
- 원인: server/index.js 히스토리 파싱에서 빈 줄 만나면 content 수집 중단 (라인 696)
  - memory/*.md는 ### 하위 섹션 사이에 빈 줄이 많아서 첫 줄만 수집됨
- 작업: 빈 줄 중단 로직 제거 → 다음 ## 섹션까지 전체 포함 (mock/real 양쪽)
- 결과: ✅ 3/31 4건, 3/30 1건 모두 500자 내용 정상 표시
- git: d87fc9ac
