
## 18:40 KST [붐코] Vision 기능 디버깅 + API 가이드

- 지시: Vision 이미지 인식 안 되는 문제 수정
- 작업:
  - 로그 분석 → tools 파라미터가 Vision 경로보다 먼저 처리되는 구조적 버그 발견
  - `server_v3_postgres_router.py`: `_has_vision` 사전 체크 추가, tools AND NOT vision일 때만 tool calling 진입
  - `server_v3_postgres_router.py`: `Union[str, List[...]]` → `Union[List[...], str]` Pydantic v2 순서 버그 수정
  - `model_router.py`: repetition_penalty=1.5, temperature≤0.3 Vision hallucination 억제
  - `booml-bot-v2-enhanced.py`: 이미지 기본 OCR 프롬프트, `_clean_vision_response()` 후처리 추가
  - `BOOML_API.md` 작성: 텍스트/Vision API 사용법 (curl + Python)
  - `feat/vision-file-analysis` 브랜치 커밋 (6개)
- 결과: Vision 경로 정상 작동 확인 (65 t/s), API 가이드 /Users/sykim/workspace/boom-workspace/booml-mlx/BOOML_API.md

## 20:20 KST [붐코] OCR 기능 추가 + 이미지 분석 완성

- 지시: 스크린샷 텍스트 인식 안 되는 문제 해결
- 작업:
  - 원인: Gemma 4 Vision은 OCR 특화 모델이 아님 → 텍스트 추출 불량
  - Tesseract 5.5.2 + tesseract-lang(kor) 설치
  - pyobjc-framework-Vision + pytesseract 설치
  - `booml-bot-v2-enhanced.py`: `_ocr_image()` 추가 (Tesseract kor+eng 우선, Apple Vision 폴백)
  - 이미지 수신 시 OCR 먼저 → 텍스트 있으면 붐엘에게 Markdown 정리 요청
  - 텍스트 없는 이미지(사진/그림)는 기존 Gemma Vision 경로 유지
  - `feat/vision-file-analysis` 브랜치 커밋
- 결과: 성공 — 실제 스크린샷 테스트 677자 추출, 6.9초 응답

## 20:35 KST [붐코] OCR 듀얼 엔진 + 로그 시스템

- 지시: Apple Vision 1순위로 변경 → 테스트 → 더 나쁨 → 병렬 실행으로 개선
- 작업:
  - Apple Vision 단독: 사진 17자, 파일 17자 (Tesseract 대비 처참)
  - 듀얼 엔진으로 변경: Apple Vision + Tesseract 병렬 실행 → 더 긴 결과 선택
  - 테스트 결과: Tesseract가 항상 승리 (사진 4192자 vs AV 145자, 파일 2464자 vs AV 17자)
  - OCR JSONL 로그 추가: /Users/sykim/.openclaw/workspace/memory/ocr_log.jsonl
    - 기록 항목: ts, image_size, engines(글자수), winner, preview(200자)
  - 커밋 2개: f3331c6 (듀얼 엔진), 68aa9d7 (JSONL 로그)
- 결과: 성공 — 로그 쌓이면 개선 방향 분석 가능

## 21:15 KST [붐코] 붐엘 회고록 작성 + 문서 정리

- 지시: 붐엘 지능화 작업 회고록 + XPS 대시보드 게시
- 작업:
  - `BOOML_RETROSPECTIVE.md` 작성: Phase 1~5 전체 + Round별 타임라인 + 인수인계 가이드
  - 기존 `retrospective-2026-04-10.md` 형식에 맞춰 재작성 (섹션 번호/표/체크리스트)
  - Mac Mini 작업 환경 명시 (헤더+푸터)
  - XPS 저장: `/home/sykim/workspace/daily-reporter-v2/docs/retrospective-2026-04-10-mac.md`
  - git 커밋 `4f37878`
- 결과: 성공

## 21:28 KST [붐코] /v1/feedback 401 수정
- 지시: 피드백 엔드포인트 401 오류 수정
- 작업: `booml-bot-v2-enhanced.py` `submit_feedback()` — Authorization 헤더 누락 추가 (line 286)
- 결과: 성공, PM2 재시작 완료

## 21:38 KST [붐코] /v1/feedback 500 수정
- 지시: 피드백 500 에러 수정
- 작업:
  - 원인: `user_sessions` 딕셔너리를 user_id→session_id(str)와 session_id→dict 두 가지로 혼용
  - feedback 루프에서 문자열 값에 `.get("user_id")` 호출 → AttributeError
  - `server_v3_postgres_router.py`: 루프 제거, `user_sessions.get(user_id)` 직접 조회로 수정
  - 커밋 cb06c77
- 결과: 성공 ({"success":true} 200 응답 확인)

## 22:00 KST [붐코] 붐엘 시간 인식 오류 수정 (3단계)
- 지시: 붐엘이 시간을 잘못 말하고 15초 걸리는 문제 수정
- 작업:
  - 원인 1: `get_realtime_data()`에 현재 시각 미포함 → parts 맨 앞에 KST 시각 추가 (커밋 3333f68)
  - 원인 2: 봇이 tools 파라미터 전달 → tool calling 분기에서 realtime_data 우회됨 → 시스템 프롬프트에 시각 직접 주입 (커밋 8b0bc16)
  - 원인 3: 기존 system 메시지(위치 정보) 있으면 주입 건너뜀 (has_system=True) → 기존 메시지 앞에 prepend로 변경 (커밋 65e9b7f)
  - 결과: API 직접 테스트 정확 (2026-04-10 21:57 KST), 응답 6.5초 (이전 15초 → 툴 호출 없이 직접 답)
- 결과: 성공
